# Проверка дизайн-системы «11 111»

Этот файл хранит не пересказ правил, а несколько задач, на которых видно,
действительно ли система направляет новое решение. Автоматические проверки
снимают механические ошибки; человеческий просмотр отвечает за композицию,
редакторскую точность и вкус.

## Сценарии

| Задача на уровне намерения | Механический контракт | Рубрика решения | Живой контроль |
| --- | --- | --- | --- |
| Обновить первый экран после изменения формата проекта | Один `h1`; смысл, дата и CTA находятся в HTML до JS; RU/EN используют общий набор фактов; видео имеет постер и паузу; нет overflow | За один просмотр понятны событие, герой, период, масштаб и действие; видео усиливает, а не несёт смысл единолично | 1440×900, 390×844, 320 px; видео, постер, reduced motion; реальный перенос RU/EN |
| Добавить или изменить этап декабрьского календаря | Один структурированный источник дат и дистанций; интервал непрерывного этапа не превращается в два заезда; сумма и накопительный итог вычисляются; локальные разряды и единицы готовы в HTML; дальняя подготовка, последние 30 дней, прохождение и архив имеют детерминированные состояния | До старта на поверхности остаются пять вершин, а точный план доступен одним предсказуемым действием; во время проекта назван и связан с картой текущий плановый этап; после финиша план не выдаётся за факт | 1440×900, 390×844 и 320 px; закрытая дальняя фаза, ближняя фаза с готовыми и неготовыми фактами, текущий этап, архив, клавиатура и 200% текста |
| Начать читать дневник и найти более раннюю запись | Сразу после заголовка раздела идёт одна запись с датой, собственным заголовком и медиа; остальные сразу доступны горизонтальной лентой после текста; визуальный порядок слева направо совпадает с Tab; текущая запись исключена из ленты; прямой адрес, Back/Forward и клавиатура выбирают нужную запись; без JS весь авторский материал доступен | На первом экране раздела видно содержание последней записи; календарная шкала, повтор заголовка и двойная нумерация не задерживают чтение; каждая запись воспринимается отдельной карточкой; дата, название и стрелка образуют одну карточку; следующий элемент виден на краю ленты; для обнаружения записей не требуется клик | RU/EN 1440×900, 390×844, 320 px и 200% текста; light/dark; прямой адрес, архив, Back/Forward, клавиатура и отказ JS; медиа проверяются отдельно |
| Улучшить путь от первого обещания к участию и свежим материалам | Результат декабря 2024 года включён в представление Виктора на первом экране и ведёт прямо к интервью об этом проекте; дата свежей записи берётся из дневника; кнопка форматов открывает сами форматы, а переход к дневнику показывает последнюю запись даже после просмотра архива | Факт и интервью воспринимаются одной группой внутри первого экрана; партнёрское действие остаётся главным; дневник сразу показывает датированную запись; название действия совпадает с содержанием после перехода | RU/EN 1440, 390 и 320 px; прямые ссылки и клавиатура; фазы проекта; light/dark и 200% текста |
| Добавить новое действие или ссылку | Используется существующий CTA-паттерн и общая SVG-иконка; доступное имя описывает результат; медиассылки имеют общее оформление подписи и одинаковую реакцию подписи и стрелки на всю карточку и клавиатурный фокус; focus видим; внешний адрес безопасен; цель аналитики объявлена в реестре | Вес действия соответствует месту в маршруте; рядом нет конкурирующего равносильного CTA; текст не сводится к «Подробнее» | Клавиатура, touch target, light/dark, проверка близости текста и иконки |
| Добавить новый визуальный материал или эффект | Используются токены и существующие media/motion-контракты; нет нового случайного raw-цвета, easing или декоративного цикла; есть fallback и reduced motion | Материал объясняет календарный ритм, усилие или состояние; не маскирует слабую иерархию и не конкурирует с реальным движением Виктора | Начало, середина и конец эффекта на desktop/mobile; статичный кадр должен оставаться полноценной композицией |

## Локальный вариант для сравнения от 9 сентября 2026

Вариант подготовлен в ветке `codex/art-direction-comparison` относительно
контрольной точки `669796c`. Выпуск включает весь согласованный вариант после
выпускных проверок и визуального контроля.

Посетитель уже знаком с Виктором и приходит за продолжением истории проекта.
Партнёрский контакт остаётся главным действием. Длина страницы сама по себе
не является дефектом; сравниваем выразительность глав, связь материалов и ясность действий.

| Задача на уровне намерения | Контракт | Критерий сравнения |
| --- | --- | --- |
| Выбрать другой эпизод подготовки | Последняя запись появляется первой; после неё постоянно видна горизонтальная лента остальных записей; прокрутка не меняет статью; выбор открывает запись, меняет URL и переводит фокус | При наведении и фокусе появляется фото из записи; на touch фото видно сразу; у каждой записи самостоятельный визуальный вес; даты и заголовки читаются; видимый край следующей карточки и две кнопки объясняют продолжение ленты; клавиатура и свайп доступны |
| Понять испытание и перейти к подготовке | Сначала первый экран, устройство испытания и календарь; затем существующая звуковая глава «Присутствие», дневник и рассказ о Викторе. Факты и авторские материалы сохранены | Дневник следует после устройства проекта; тёмная звуковая глава отделяет его от светлого календаря, а после дневника начинается тёмный портрет Виктора |
| Открыть полный календарь | Нативное раскрытие, фазовые подписи и данные сохранены; действие оформлено общей контрастной поверхностью без рамки из нескольких линий | Кнопка воспринимается как действие; после нажатия начало календаря видно под ней в том же окне; клавиатурный фокус остаётся на кнопке; автоматическое открытие по фазе не прокручивает страницу; рядом нет повторных рамок |
| Увидеть первый экран целиком | На достаточно высоком широком экране пять вершин входят в высоту окна вместе с заголовком и действиями; на телефоне заголовок, действие и отсчёт образуют первый экран, а полный профиль следует отдельной полосой; короткий desktop может скрыть профиль | Внизу нет обрезанного фрагмента графика; на телефоне видны все пять вершин; счётчик не получает отдельную плашку с пустотой; при 200% содержание растёт по высоте и не подрезается |
| Перейти через меню к нужному материалу | Восемь разделов включают дневник и следуют фактическому порядку страницы; «Проект» ведёт к началу; каждая ссылка закрывает меню, меняет URL и оставляет фокус у цели | Текущий раздел отмечен подчёркиванием, наведение — цветом и соответствующим кадром; это различимые состояния; карта меню и футер используют один список |
| Различить главу дневника и отдельную запись | Номер главы вычисляется из того же списка, что меню и подвал; крупная Micra обозначает главу, заголовки всех статей используют основной текстовый шрифт и обычный регистр; Telegram сгруппирован с рубрикацией | Один сильный заголовок обозначает вход в главу; дата и название статьи начинают спокойный текстовый слой; на широком экране заголовок главы и текст статьи стоят на общей оси; на узком служебная группа не смешивается с датой записи |
| Понять цель нового испытания | После первого экрана возвращён авторский тезис «Показать, что человек может больше, чем думает»; устройство заездов объясняется рядом с календарём | Видеокадр мягко затемняется к фону главы; шапка с номером и крупный тезис сохраняют её самостоятельность; движение не требуется для понимания |
| Рассмотреть фотосерию Виктора | Все шесть эпизодов возвращены в исходном порядке и масштабе; файлы, даты и авторство прежние | Крупные кадры и паузы сохраняют ритм; серия не сокращается ради уменьшения высоты страницы |
| Оценить опыт Виктора | Четыре достижения, фотография, сообщества и возраст сохранены; авторский дневник не меняется | Локальная редактура вводного текста и снятие цитатной плашки остаются предметом сравнения с production |
| Выбрать роль партнёра и начать разговор | Три направления; краткое описание начала работы рядом с контактом; каналы и ссылка на результаты сохранены | На телефоне имя и два канала связи образуют одну группу: между именем и кнопками меньше места, чем до вводного текста; кнопки видны без наведения; ссылка на результаты подчинена контакту по весу |

Визуальный контроль: RU/EN 1440×900 и 390×844; дополнительно 320 px, 200% текста,
клавиатура, обычное и сокращённое движение, тёмная тема и отключённый JS.

## Чтение длинной записи

Задача: дочитать полный авторский текст, продолжая видеть приложенное медиа,
и перейти к архиву. У короткой записи фото и текст прокручиваются вместе.
На широком экране левая галерея закрепляется в пределах статьи, только когда
текст продолжается за её нижним краем больше чем на половину высоты окна;
высота галереи включает кнопки и миниатюры. Высокая галерея сначала прокручивается
до нижнего края и лишь затем закрепляется. В конце статьи она уходит вместе с ней.
На телефоне и при 200% текста — обычный поток. Отдельной прокрутки текста нет.
Проверка поведения находится в `checkDiaryReadingRoute`; дополнительно смотреть
середину и конец длинной записи, низкую высоту окна и галерею с несколькими медиа.

## Свет места

Задача: почувствовать декабрьский Дубай через свет страницы и при желании
пройти день от рассвета к ночи. Утро, полдень, закат и ночь должны различаться
на тех же фотографиях и поверхностях; основные тексты, партнёрское действие,
смена глав и явный выбор темы сохраняют читаемость и роль.

Режим «День старта» показывает расчёт солнца на дату из календарной модели,
а «Сейчас» — местное время подтверждённой трассы. Будущая погода не выдумывается. Свежие
погодные данные влияют на мягкость света; при их отсутствии солнечная модель
продолжает работать с явной подписью. Восход и закат одной даты не меняются при
перемещении ползунка. Никакой слой не изображает фактическое положение Виктора.

Контроль: `scripts/dubai-light-check.mjs`; RU/EN, Chromium/WebKit, light/dark,
1440, 390, 320 px и 200% текста, клавиатура, reduced motion, отсутствие JS,
недоступное хранилище, свежий/устаревший/недоступный источник. Дополнительно
смотреть переход видео → вершины → цель проекта и финальный знак в разные часы.
Название города не повторяется в соседних строках. Дата относится к часам,
время прогноза и ссылка на источник стоят рядом одной служебной группой;
резерв длинного fallback не раздвигает их. На дневном рендере
видны белый и светлый песок, на закате — боковой золотой свет и длинная тень,
ночью — холодная среда. Разницу оценивать на одинаковом кадре, а не по токенам.
Основания расчёта, подтверждение места и условия погодного источника описаны в
`docs/dubai-light.md`.

При сравнении дня старта с текущим временем кнопки, ползунок и финальный знак
сохраняют положение, в том числе при переносах подписей на 320 px / 200%.
У вершин остаётся только подпись о дистанции; город и часы здесь не дублируются.

Открывая меню, посетитель видит сегодняшнюю сводку для трассы независимо от
выбранного дня светового просмотра. Проверять температуру, направление ветра,
облачность, час прогноза и состояние отказа; данные не должны вытеснять пункты
навигации или партнёрское действие. При +40 °C появляется текстовая отметка
жары. Погода читается текстовой сводкой, а не таблицей: подписи и значения
стоят в строку, рядом с местом и явно подписанным часом прогноза. Например,
в 18:21 прогноз на 18:00 не должен читаться как часы с текущим временем.
Ссылка на источник остаётся в блоке «День в Дубае». При нехватке ширины
переносятся цельные пары, а не отдельные подписи. Над сводкой — короткая
строка отсчёта: весь атом «80 дней» набран Commissioner 600, 30–32 px и лаймом,
«до старта» — служебным кеглем. Первый экран сохраняет крупный отсчёт Micra.
На мобильном отсчёт крупнее — 40–46 px при обычном размере текста; от погоды
его отделяет внешний интервал 1,5 rem. Дата прогноза и погодные пары остаются
в одной компактной группе, которая переносится по доступной ширине.
UV, температура и ветер меняют только фон; проверить обычное движение,
общую паузу с видео и reduced motion. Пыль без собственного источника не
имитировать. NASA GEOS-FP отдельно задаёт пылевую вуаль только на медиа;
устаревший или повреждённый снимок выключает эффект. В режиме дня старта
современный прогноз не применяется. Источник и время держатся одной группой.

### Продолжающаяся история

| Задача посетителя | Проверяемое поведение | Визуальный критерий |
| --- | --- | --- |
| Увидеть, что произошло с прошлого визита | Первый визит без уведомления; новая запись появляется до её просмотра; отказ хранилища не ломает страницу | Короткая полоса ведёт к самому материалу и не конкурирует с главным действием |
| Понять, сколько подтверждено командой | Пустая история до старта; монотонные датированные отметки; источники доступны; между отметками нет экстраполяции | Постоянный след отличается от плана; итог и дата читаются рядом |
| Посмотреть, как проходил прежний заезд | GPX и 203 отрезка Strava согласованы; маршрут виден без раскрытия и JS; загрузка не сдвигает управление; подпись фазы остаётся на месте при смене минут, суток и всех четырёх фаз; перемотка, конец, пауза, уход с экрана и отказ загрузки; обе локали и 320 px / 200% | После календаря заметен исторический эпизод с датой; свет занимает всю ширину страницы, граница с календарём чёткая и без промежуточной серой полосы, текст стоит на общей оси; заголовок, маршрут, цифры и запуск читаются одной композицией; свет меняется по восстановленному времени; приблизительность положения явно названа; схема не выдаётся за текущую позицию Виктора |

Контроль данных — `scripts/journey-check.mjs`; рендеры и действия — Chromium
и WebKit на 1440, 390 и 320 px, включая 200% текста и reduced motion.

## Уровни доказательства

1. **Данные и сборка.** Факты, формулы, локали и обязательные поля согласованы.
2. **Статический контракт.** Семантика, токены, типографика, ссылки и состояния
   проверены по фактическому HTML/CSS/JS, а не только поиском строки.
3. **Поведение и доступность.** Chromium/WebKit, клавиатура, reflow, 200%,
   reduced motion и отказ медиа.
4. **Визуальный кандидат.** Реальные рендеры нужного места на desktop/mobile,
   включая крайние состояния.
5. **Человеческая приёмка.** Иерархия, близость, редакторская точность и общий
   ритм страницы признаны убедительными.
6. **Production.** Публичный URL отдаёт точные ассеты и воспроизводит принятый
   результат без старого кэша.

Нижний уровень не доказывает следующий: зелёная сборка не является визуальной
приёмкой, а удачный рендер локально не является подтверждением production.

## Когда расширять eval-контур

Полный prompt-eval suite нужен, если одновременно выполняются три условия:

- компоненты и токены стабилизировались и используются многократно;
- агент регулярно получает похожие UI-задачи, в которых уже проявляется
  повторяемая деградация;
- проверки можно поддерживать и запускать в CI без ручной подготовки.

До этого сценарии выше служат общей рамкой для `src/check.mjs`, браузерных
регрессий, screenshot gate и человеческого визуального контроля. Если всё же
оценивается именно поведение агента, одну и ту же естественную задачу запускают
несколько раз: единичный удачный результат не доказывает устойчивость правил.
